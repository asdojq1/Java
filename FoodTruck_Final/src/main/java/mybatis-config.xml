<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
  
<configuration>
	<!-- 우리가 만들 mapping 폴더안에 작성될 쿼리문들을 연결하는 문서 -->
	<!-- 우리가 사용하는 클래스변수(VO)에 대한 별칭(변수) -->
	<typeAliases>
		<typeAlias type="com.java.test.domain.MemberVO" alias="member" />
	</typeAliases>

	<!-- 우리가 작성한 sql 을 싱행하기 위한 설정 -->
	<!-- <mapper resource=""/> 호출하여 시스템에 등록 -->
	<!-- <mappers> <mapper resource="mappers/LoginMapper.xml"/> </mappers> -->
</configuration>